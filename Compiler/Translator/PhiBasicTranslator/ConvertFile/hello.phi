#

Hello world program
str message: 'hello, world\r\n';
#

phi.Hello:OS16BIT
{
	str message: 'hello, world\r\n';
	int value: 44;

	log message 'test' value;

	while int i: 0; i < 10; i++;

		log message;

		if	i < 5
			log i;
		;;
    ;;

}


#

		if(4 * y + (t/2) is (67 or 84) + y) and value > 5:

			log 'very complex conditional';

		;;

		if(value: 8 * y):

		if(str test: 'hello')

		;;
	

#

