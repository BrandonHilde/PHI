phi.Hello:OS16BIT
{
	str hello: 'Hello, World!\r\n';
	str large: 'Large Number\r\n';
	str testv: 'testsing';

	while int i: 0; i < 10; i++;
	
		log	hello;
		
		if		i > 4
			log large;
		;;
    ;;
	
	call Test;
}

asm.Test
{
	mov si, {testv}
	call print_log
	ret
}