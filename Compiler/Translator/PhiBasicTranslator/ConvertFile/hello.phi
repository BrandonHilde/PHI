phi.Hello:OS16BIT
{
   	str newline: '\r\n';
	int i: 12;
	int y: 7;
	int t: 3;
	
	int val: 0;
	
	log 'test';

	val is ((5 + (y * 3) - 3) / (t + 1));
	
	val %% 3;
	
	log 'hello';
	log newline;
	log val;
	
	
}